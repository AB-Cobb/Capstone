export class Teammate {
    public id : number;
    public name : string;
    public active : boolean;

    constructor(id: number, name : string, active : boolean) {
        this.id = id;
        this.name =name;
        this.active = active;
    }
    
}